#!/usr/bin/make -f
export DEB_BUILD_MAINT_OPTIONS = hardening=+all
%:
	dh $@ --buildsystem=cmake
override_dh_auto_configure:
	dh_auto_configure -- -DUSE_QT5=ON -DBUILD_TESTER=OFF -DBUILD_TESTS=OFF
override_dh_strip:
	dh_strip --no-automatic-dbgsym
